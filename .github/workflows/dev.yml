name: Dev Environment Test

on:
  push:
    branches:
      - dev

jobs:
  dev-job:
    runs-on: ubuntu-latest

    steps:
      - name: Say hello from DEV
        run: echo "👋 Hello from DEV environment"
